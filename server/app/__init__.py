"""Heirs of the Abyss game server.

Deterministic soulslike engine + LangGraph dungeon master + RAG.
Wave 1 scaffold only — `main`, `game`, `agent`, and `rag` modules land in
subsequent todos (T1.3 onward).
"""

__version__ = "0.1.0"
