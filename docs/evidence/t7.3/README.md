# T7.3 evidence — flee resolution + reject cap

> [!TIP]
> `test_reject_cap_forces_flee`: wrong hash #1 → `verified:false` with `fail_count==1` (persisted); #2 → flee (`outcome.reason=reject_cap`, telemetry hook fired). `test_tick_limit_forces_flee`: input past `FIGHT_TICK_LIMIT` → flee `reason=tick_limit`. No rewards on any flee.
