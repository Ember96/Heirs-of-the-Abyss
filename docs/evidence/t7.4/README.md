# T7.4 evidence — boss gate

> [!TIP]
> `test_boss_gate_blocks_descend_until_defeated`: descend off floor 5 with boss alive → `rule_violation`; after `bosses_defeated.append("the_violence")` descend succeeds (floor 6). Wins record the boss id via `apply_fight_result`.
