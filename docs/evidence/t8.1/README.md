# T8.1 evidence — vendored CC0 art + side-view arena

> [!TIP]
> 160 files vendored into `client/assets/art/` (Gothicvania church/cemetery, hero frames, CREDITS.md). `floor_renderer.gd` rewritten as side-view gothic arena behind the SAME node/API (`render_floor(seed,cols,rows)`); legacy iso TileMapLayers freed at runtime; `static floor_layout` preserved byte-for-byte (harness: `deterministic=true cells=96 walls=44`). Headless import clean; scene smoke silent.
