# T7.2 evidence — permadeath

> [!TIP]
> `test_death_sets_terminal_and_emits_game_over`: scripted loss streams ~150 ticks, honest submit → frames tail `[fight_result(verified:true, php<=0), game_over(reason=death)]`; `session.terminal` true in memory AND in the store; fight row `lost`.
